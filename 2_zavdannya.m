% Крок 6: Побудова графіку
figure;

% Побудова вхідних векторів з відповідними класами
h0 = plot(X_normalized(1,T==0), X_normalized(2,T==0), 'ro'); hold on;
h1 = plot(X_normalized(1,T==1), X_normalized(2,T==1), 'b+');

% Побудова розділяючої гіперплощини
hline = plotpc(net.IW{1,1}, net.b{1}); % Повертає line-графік

% Налаштування графіку
title('Класифікація персептроном (Варіант 2)');
xlabel('Ознака 1 (нормована)');
ylabel('Ознака 2 (нормована)');
grid on;

% Побудова легенди
legend([h0(1), h1(1), hline], {'Клас 0', 'Клас 1', 'Розділяюча лінія'});
